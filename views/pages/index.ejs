<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="styles/base.css">
    <link rel="stylesheet" href="styles/layout.css">
    <link rel="stylesheet" href="styles/modules.css">
    <link rel="stylesheet" href="styles/reset.css">
    <title>HOMEPAGE</title>
</head>
<body>
    <h1> HOMEPAGE </h1>

      <h2>Total Books: <%=books.length%></h2>
      <ul>
        <ul>
          <% for(let book of books){ %>
          
            <li>
              <div>
                <h3><%= book.bookTitle %></h3>
                <img src='<%= book.img%>'>
                <p><%= book.authors %></p>
                <p><%= book.book_description %></p>
              </div>

              <form action='/books/<%= book.id %>' method='GET'>
                <input type='hidden' name='authors' value='<%= book.authors%>'>
                <input type='hidden' name='img' value='<%= book.img%>'>
                <input type='hidden' name='bookTitle' value='<%= book.bookTitle%>'>
                <input type='hidden' name='book_description' value='<%= book.book_description%>'>
                <input type='hidden' name='isbn' value='<%= book.isbn%>'>
                <input id="form_button" type='submit' value ='View Details'>
              </form>
            </li>
            
            <% } %>
      </ul>


</body>
</html>